# Infrastructure

This folder groups local infrastructure assets used by the monorepo:

- `postgres/`
- `redis/`
- `minio/`

The MVP runs with PostgreSQL as the required service. Redis and MinIO stay as prepared placeholders for later phases.
