# App Layer

This folder contains the mobile application shell:

- `app.dart`: root app composition
- `routes/`: navigation setup
- `theme/`: visual tokens and theme configuration
- `config/`: environment-facing mobile configuration
